export enum DefaultColumKeys {
    // orderId, task, station, startAt, finishAt, tardiness, setupDuration, processingDuration, dueAt
    orderId = 'orderId',
    task = 'task',
    station = 'station',
    startAt = 'startAt',
    finishAt = 'finishAt',
    tardiness = 'tardiness',
    setupDuration = 'setupDuration',
    processingDuration = 'processingDuration',
    duration = 'duration',
    dueAt = 'dueAt',
    note = 'note'
}
